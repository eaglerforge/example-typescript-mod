{
  "name": "example-typescript-mod",
  "version": "1.0.0",
  "description": "Example typescript mod for eaglerforge.",
  "scripts": {
    "build": "bun build --bundle ./src/main.ts --outdir=out/ --outfile=out/plugin.js --sourcemap=inline --watch --minify"
  },
  "author": "your name",
  "devDependencies": {
    "css-loader": "^6.8.1",
    "ts-loader": "^9.4.4"
  }
}
